import { useState } from "react";
import { motion } from "framer-motion";
import { Eye, EyeOff, KeyRound, ShieldCheck, X } from "lucide-react";

type AuthView = "login" | "first-access" | "reset";

const field = "w-full rounded-xl border border-stone-600 bg-stone-950/70 px-4 py-3 text-sm text-stone-100 outline-none transition focus:border-cyan-300 focus:ring-2 focus:ring-cyan-300/20";

export function AuthFlowPreview() {
  const [view, setView] = useState<AuthView>("login");
  const [showPassword, setShowPassword] = useState(false);
  const [remember, setRemember] = useState(true);
  const [notice, setNotice] = useState("");
  const submit = (message: string) => { setNotice(message); window.setTimeout(() => setNotice(""), 2600); };
  return <main className="kd-ambient min-h-screen bg-stone-950 px-4 py-10 text-stone-100 sm:px-8"><div className="mx-auto flex min-h-[calc(100vh-5rem)] max-w-5xl flex-col items-center justify-center gap-6"><header className="text-center"><p className="font-serif text-2xl font-bold tracking-[0.45em]">KINGDOOM</p><p className="mt-2 text-sm text-stone-400">SimulaciÃ³n de acceso seguro</p></header>{view === "login" && <motion.section initial={{ opacity: 0, y: 18, scale: 0.98 }} animate={{ opacity: 1, y: 0, scale: 1 }} transition={{ duration: 0.35 }} className="w-full max-w-md rounded-3xl border border-stone-700/70 bg-stone-900/90 p-7 shadow-2xl backdrop-blur-xl sm:p-9"><div className="mx-auto mb-5 grid size-14 place-items-center rounded-full bg-amber-400/15 text-amber-300"><KeyRound size={27} /></div><h1 className="text-center text-2xl font-bold">Iniciar sesiÃ³n</h1><p className="mt-2 text-center text-sm text-stone-400">ContinÃºa tu aventura en el reino</p><form className="mt-8 space-y-5" onSubmit={(event) => { event.preventDefault(); submit("Vista previa: credenciales listas para conectar con Supabase Auth."); }}><label className="block text-sm text-stone-300">Nombre de usuario<input className={`${field} mt-2`} defaultValue="Arthan" autoComplete="username" /></label><label className="block text-sm text-stone-300">ContraseÃ±a<div className="relative mt-2"><input className={`${field} pr-12`} type={showPassword ? "text" : "password"} defaultValue="kingdoom-demo" autoComplete="current-password" /><button type="button" aria-label="Mostrar contraseÃ±a" className="absolute right-3 top-3 text-stone-400" onClick={() => setShowPassword((value) => !value)}>{showPassword ? <EyeOff size={18} /> : <Eye size={18} />}</button></div></label><div className="flex items-center justify-between gap-3 text-xs"><label className="flex items-center gap-2 text-stone-300"><input type="checkbox" checked={remember} onChange={(event) => setRemember(event.target.checked)} className="accent-amber-400" />Recordar usuario</label><button type="button" className="text-cyan-300" onClick={() => setView("reset")}>Â¿Olvidaste tu contraseÃ±a?</button></div><button className="w-full rounded-xl bg-amber-400 px-4 py-3 font-bold text-slate-950">Entrar</button></form><button type="button" className="mt-5 w-full text-center text-sm text-cyan-300" onClick={() => setView("first-access")}>Â¿Primera vez? Configura tu acceso</button></motion.section>}{view === "first-access" && <motion.section initial={{ opacity: 0, y: 18 }} animate={{ opacity: 1, y: 0 }} className="w-full max-w-xl rounded-3xl border border-stone-700/70 bg-stone-900/90 p-7 shadow-2xl backdrop-blur-xl sm:p-9"><div className="flex items-center justify-between"><div><p className="text-xs font-semibold uppercase tracking-widest text-cyan-300">Paso 1 de 2 Â· VerificaciÃ³n</p><h1 className="mt-3 text-2xl font-bold">Configura tu contraseÃ±a</h1></div><button aria-label="Cerrar" onClick={() => setView("login")} className="text-stone-400"><X size={20} /></button></div><p className="mt-2 text-sm text-stone-400">Tu cuenta ya existe; confirma tu identidad para crear el acceso.</p><form className="mt-7 grid gap-4 sm:grid-cols-2" onSubmit={(event) => { event.preventDefault(); submit("Vista previa: contraseÃ±a creada y lista para enlazar."); }}><label className="text-sm text-stone-300 sm:col-span-2">Usuario vinculado<input className={`${field} mt-2`} value="Arthan" readOnly /></label><label className="text-sm text-stone-300">Nueva contraseÃ±a<input className={`${field} mt-2`} type="password" placeholder="MÃ­nimo 8 caracteres" required /></label><label className="text-sm text-stone-300">Repite la contraseÃ±a<input className={`${field} mt-2`} type="password" placeholder="Repite la contraseÃ±a" required /></label><label className="text-sm text-stone-300 sm:col-span-2">CÃ³digo de verificaciÃ³n de WhatsApp<input className={`${field} mt-2 tracking-[0.35em]`} inputMode="numeric" placeholder="123456" required /></label><button className="mt-2 rounded-xl bg-amber-400 px-4 py-3 font-bold text-slate-950 sm:col-span-2">Guardar contraseÃ±a</button></form></motion.section>}{view === "reset" && <motion.section initial={{ opacity: 0, y: 18 }} animate={{ opacity: 1, y: 0 }} className="w-full rounded-3xl border border-stone-700/70 bg-stone-900/90 p-7 shadow-2xl backdrop-blur-xl sm:p-9"><div className="flex items-center gap-3"><ShieldCheck className="text-cyan-300" /><div><p className="text-xs font-semibold uppercase tracking-widest text-cyan-300">Admin Staff Â· Seguridad</p><h1 className="mt-1 text-2xl font-bold">Restablecer contraseÃ±a</h1></div></div><div className="mt-7 grid gap-6 lg:grid-cols-[1fr_360px]"><div className="space-y-3">{["Arthan", "LunaRojiza", "Kael"].map((username) => <div key={username} className="flex items-center justify-between rounded-xl border border-stone-700 bg-stone-800/70 px-4 py-3"><p className="font-semibold">{username}</p><button onClick={() => submit(`Restablecimiento preparado para ${username}.`)} className="rounded-lg bg-cyan-300/15 px-3 py-2 text-xs font-semibold text-cyan-200">Restablecer</button></div>)}</div><form className="rounded-2xl border border-cyan-300/60 bg-stone-800/80 p-5" onSubmit={(event) => { event.preventDefault(); submit("Vista previa: acciÃ³n registrada en auditorÃ­a."); }}><p className="font-semibold">Usuario: Arthan</p><label className="mt-5 block text-sm text-stone-300">Nueva contraseÃ±a temporal<input className={`${field} mt-2`} type="password" required /></label><label className="mt-4 flex items-center gap-2 text-sm text-stone-300"><input type="checkbox" defaultChecked className="accent-amber-400" />Revocar sesiones activas</label><button className="mt-6 w-full rounded-xl bg-amber-400 px-4 py-3 font-bold text-slate-950">Confirmar restablecimiento</button><button type="button" onClick={() => setView("login")} className="mt-4 w-full text-sm text-stone-400">Cancelar</button></form></div></motion.section>}<nav className="flex gap-2 text-xs text-stone-400"><button onClick={() => setView("login")}>Inicio</button><span>Â·</span><button onClick={() => setView("first-access")}>Primer acceso</button><span>Â·</span><button onClick={() => setView("reset")}>Staff</button></nav>{notice && <div role="status" className="fixed bottom-5 rounded-xl border border-cyan-300/40 bg-slate-900 px-4 py-3 text-sm text-cyan-200 shadow-xl">{notice}</div>}</div></main>;
}



